name: Handle issues
on: issues
jobs:
  output-info:
    runs-on: ubuntu-latest
    steps:
      - name: Output issue information or output events details
        runs: echo "${{ toJson(github.event) }}"
        