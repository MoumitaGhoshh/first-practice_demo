name: Lint, Test, Build, Deploy (Multi job)

on:
  push:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout to get code into runner machine
        uses: actions/checkout@v5

      - name: Set up or install node.jobs
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies to available into runner machine
        run: npm ci

      - name: Run Lint
        run: npm run lint

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout code to get code into runner machine
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies to available into runner machine
        run: npm ci

      - name: Run tests code
        run: npm run test

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code or get code into runner machine
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies to available into runner machine
        run: npm ci

      - name: Build the project or code
        run: npm run build

      - name: Deploy or Simulate the project
        run: echo "Deployment simulated successfully!"